# API Server Scaffold (Healthcheck Only)

> [!WARNING]
> This directory (`guide/artifacts/api-server`) contains a mockup/scaffold that only implements the `/healthz` endpoint.
> **DO NOT** deploy this as the production backend API. The actual production API lives externally and must be configured via the `VITE_API_URL` environment variable.
